{
  "name": "@oxus-examples/minimal-python-provider",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal Python provider module — push this tree alone to a git repo for pm add --type git.",
  "scripts": {
    "build": "oxus build --no-interactive"
  },
  "devDependencies": {
    "oxus-framework": "file:../../oxus-framework"
  }
}
