# Project Overview

1 modules · 1 capabilities · 0 entitlements

## Modules

### minimal-py-provider (v0.0.1)

Minimal Python provider — one hello capability (for git + Oxus pm demos).

**Provides:** `hello`


## Quick Start

```bash
pnpm install
pnpm -r --filter './submodules/*' exec oxus build
```

Run `oxus build` after editing any `module.yaml`. Per-module detail in `generated/SKILL.md` or `CLAUDE.md`.
