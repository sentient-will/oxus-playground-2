---
name: minimal-py-provider
description: Test skill 1 — one hello capability. Provides: hello. Use when implementing or invoking: `hello`.
metadata:
  oxusModule: "minimal-py-provider"
  oxusVersion: "0.0.1"
  oxusCapabilities: "hello"
---

# minimal-py-provider (v0.0.1)

Test skill 1 — one hello capability.


## Executable skills (scripts)

Named argv-based commands declared in **`module.skills.executableSkills`**. Run from the repo root with the Oxus CLI (uses the resolved module root, including git lockfile providers after `oxus pm sync`):

- **`hello-cli`** (risk: **low**) — `oxus skills run minimal-py-provider.hello-cli`
  - Print a greeting to stdout; optional name as first arg after `oxus skills run … --`.
  - Command: `python3 ./scripts/hello_cli.py` · cwd: `.` (relative to module root)
  - Related capability: `hello`

Optional allowlist: **`.oxus/skills-allow.yaml`** (see `oxus skills run --help`). Extra arguments after `--` are appended to the skill argv.


## Capabilities

**`hello`** — Returns a greeting for a name.
- Handler: `./src/capabilities/hello.py`
- Request: `{ name: string }`
- Response: `{ message: string }`

## Source files

- `./src/capabilities/hello.py`

---

*Auto-generated by Oxus. Edit `module.yaml` or the architecture source (`module.skills`); do not edit this file by hand.*

*Mirrored to agent discovery paths (same content):* `.cursor/skills/minimal-py-provider/SKILL.md` · `.claude/skills/minimal-py-provider/SKILL.md` · `skills/minimal-py-provider/SKILL.md`
