# Project — 1 modules

- **minimal-py-provider** (v0.0.1) — Minimal Python provider — one hello capability (for git + Oxus pm demos). · provides: `hello`

Per-module detail: `.cursor/rules/{module}.mdc` (Cursor) or `{moduleDir}/CLAUDE.md` (Claude) — auto-loaded when editing that module.
Run `oxus build` after editing any `module.yaml`.
