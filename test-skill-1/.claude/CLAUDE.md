# Project — 1 modules

- **test-skill-1** (v0.0.1) — Test skill 1 — one hello capability. · provides: `hello`

Per-module detail: `.cursor/rules/{module}.mdc` (Cursor) or `{moduleDir}/CLAUDE.md` (Claude) — auto-loaded when editing that module.
Run `oxus build` after editing any `oxus.yaml`.
