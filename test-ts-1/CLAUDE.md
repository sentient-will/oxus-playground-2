# minimal-ts-provider (v0.0.1)
Minimal TypeScript provider — one hello capability.
Provides: `hello`.
Full merged skill: `.claude/skills/minimal-ts-provider/SKILL.md` (same content as `generated/SKILL.md`).
Run `oxus build` after editing `module.yaml`.
