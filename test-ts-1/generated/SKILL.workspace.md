---
name: workspace
description: Start here — Oxus workspace map and where to read per-module skills (architecture + APIs).
---

# Workspace (Oxus)

**Full map:** `generated/module-index.md` — all modules, capabilities, entitlements, and Mermaid diagram.

## Reading order

1. **`generated/module-index.md`** — topology and which modules exist.
2. **Per module** — `.cursor/skills/<module-id>/SKILL.md` (mirrors: `.claude/skills/<module-id>/SKILL.md`, `skills/<module-id>/SKILL.md`) for merged architecture + capabilities + entitlements.
3. **Integrations** — if a module **consumes** other modules via entitlements, open that module’s `generated/SKILL.md` and use **Provider skills (lockfile-resolved)** for provider roots and paths (after `oxus build`).


---

*Auto-generated by Oxus. Do not edit.*
